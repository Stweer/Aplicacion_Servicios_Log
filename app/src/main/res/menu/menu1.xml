<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:checkableBehavior="single">
        <item android:id="@+id/MiCuenta"
            android:icon="@drawable/ic_profile"
            android:title="Mi Perfil" />

        <item android:id="@+id/CrearServicio"
            android:icon="@drawable/ic_pend"
            android:title="Crear Servicio" />

        <item android:id="@+id/MisServicios"
            android:icon="@drawable/ic_pendientes"
            android:title="Mis Servicios" />

        <item android:id="@+id/MisPendientes"
            android:icon="@drawable/ic_pend"
            android:title="Pendientes" />
    </group>
    <item android:title="Comunicate con nosotros"
        android:iconTint="@android:color/white">
        <menu>
            <item
                android:id="@+id/AcercaDe"
                android:icon="@drawable/ic_pendientes"
                android:title="Acerca de ..." />

            <item android:id="@+id/nav_compartir"
                android:icon="@drawable/ic_compartir"
                android:title="Compartir" />

        </menu>
    </item>
    <item android:title="Acciones adicionales">
        <menu>

            <item android:id="@+id/nav_salir"
                android:icon="@drawable/ic_salir"
                android:title="Salir" />
        </menu>
    </item>

</menu>
